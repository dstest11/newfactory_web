# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Strapi env stubs — tests run with a placeholder token so StrapiContentClient
# short-circuits to null (see ::fetchCached check for token starting with '__').
# This makes repositories fall through to their hardcoded fallback copy, which
# is what tests assert on. No outbound HTTP from the test suite.
NEWFACTORY_STRAPI_URL=http://strapi.invalid
NEWFACTORY_STRAPI_API_TOKEN=__test_stub__
NEWFACTORY_SENTRY_DSN=
NEWFACTORY_LEAD_RECIPIENT=test@example.com
NEWFACTORY_MAIL_HTTP_API_URL=http://mail.invalid
NEWFACTORY_MAIL_RELAY_API_KEY=__test_stub__

# Editor allowlist for security firewall tests. The password "edit-test-2026"
# hashes to the bcrypt below (cost 4, generated specifically for the test env).
NEWFACTORY_EDITOR_EMAILS=editor@example.com
# bcrypt of "edit-test-2026" (cost 4 — see when@test override in security.yaml).
# Symfony's dotenv parser treats `$` literally inside unquoted values when the
# next char isn't a valid var-name start; bcrypt hashes always begin "$2y$..."
# which is safe to leave unquoted.
NEWFACTORY_EDITOR_PASSWORD_HASH='$2y$04$HBMggiTuev44JtxLxfEm7.o9yrKPxjoM2TUQjKDXTSSyvbgqLCvOO'
